# 当前项目简介

```markdown
Python Manager - Python 环境管理 GUI 工具集
功能：PyInstaller 打包、环境克隆、依赖分析、便携式 Python 打包
技术栈：Python + ttkbootstrap + PyInstaller
架构：六层架构（l0_resource → l5_atom）
```

# 必读文档

- **当前项目简介**：

  ```bash
  README.md / README_CN.md
  ```
- **身份与角色**：

  ```bash
  Python 桌面 GUI 应用项目
  ```
- **编码规范**：

  ```bash
  六层架构：l0_resource(常量/i18n) → l1_entry(入口) → l2_gui(界面) → l3_coordinator(协调) → l4_molecule(功能) → l5_atom(工具)
  ```
